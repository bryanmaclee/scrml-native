# scrml-native

Pure self-host of the **scrml** compiler — every module written in scrml. Dormant during the split phase; the working (TypeScript/JavaScript) compiler is the sibling [`scrml`](../scrml) repo, which is authoritative until self-hosting completes. Renamed from `scrml`→`scrml-native` at S200 (2026-06-16).
